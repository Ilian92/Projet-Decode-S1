{% extends 'base.html.twig' %}

{% block title %}Address index{% endblock %}

{% block body %}
    <h1>Address index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>RecipientName</th>
                <th>Street</th>
                <th>AddressDetails</th>
                <th>PostalCode</th>
                <th>City</th>
                <th>Country</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for address in addresses %}
            <tr>
                <td>{{ address.id }}</td>
                <td>{{ address.label }}</td>
                <td>{{ address.recipientName }}</td>
                <td>{{ address.street }}</td>
                <td>{{ address.addressDetails }}</td>
                <td>{{ address.postalCode }}</td>
                <td>{{ address.city }}</td>
                <td>{{ address.country }}</td>
                <td>
                    <a href="{{ path('app_admin_address_show', {'id': address.id}) }}">show</a>
                    <a href="{{ path('app_admin_address_edit', {'id': address.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_address_new') }}">Create new</a>
{% endblock %}
