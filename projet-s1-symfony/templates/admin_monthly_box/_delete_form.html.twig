<form method="post" action="{{ path('app_admin_monthly_box_delete', {'id': monthly_box.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monthly_box.id) }}">
    <button class="btn">Delete</button>
</form>
