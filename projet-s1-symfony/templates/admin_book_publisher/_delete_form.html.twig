<form method="post" action="{{ path('app_admin_book_publisher_delete', {'id': book_publisher.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_publisher.id) }}">
    <button class="btn">Delete</button>
</form>
